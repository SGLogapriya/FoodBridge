name: Build and deploy Node.js app to Azure Web App - foodbridge-backend-api

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'

      # ✅ Install dependencies inside backend folder
      - name: Install dependencies
        working-directory: ./foodbridge-backend
        run: npm install

      # ✅ Upload correct backend folder
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: ./foodbridge-backend

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      # ✅ Download artifact properly
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app
          path: .

      # ✅ Deploy to Azure
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'foodbridge-backend-api'
          package: .
